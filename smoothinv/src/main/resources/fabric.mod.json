{
  "schemaVersion": 1,
  "id": "smoothinv",
  "version": "${version}",
  "name": "SmoothInv",
  "description": "Caches item tooltip lines instead of rebuilding them every frame, reducing allocations while the inventory screen is open. Optionally skips rendering the inventory player model.",
  "authors": [
    "yxusuf05"
  ],
  "license": "MIT",
  "icon": "assets/smoothinv/icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "dev.smoothinv.SmoothInv"
    ]
  },
  "mixins": [
    "smoothinv.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}"
  }
}
